<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta http-equiv="X-UA-Compatible" content="ie=edge" />
    <title>Order History</title>

    <!-- Google Font -->
    <link href="https://fonts.googleapis.com/css2?family=Nunito+Sans:wght@300;400;600;700;800;900&display=swap" rel="stylesheet" />
    
    <!-- Font Awesome Icons -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
    
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">

       <!-- Custom CSS -->
    <style>
        body {
            font-family: 'Nunito Sans', sans-serif;
            background-color: #f8f9fa;
        }
        .dashboard-card {
            background-color: #ffffff;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            padding: 20px;
        }
        .nav-link {
            color: #333;
            font-weight: 600;
            margin-bottom: 10px;
        }
        .nav-link i {
            margin-right: 10px;
        }
        .nav-link:hover {
            background-color: #f1f1f1;
            border-radius: 8px;
            color: #007bff;
        }
        .order-card {
            background-color: #ffffff;
            border: 1px solid #e0e0e0;
            border-radius: 10px;
            margin-bottom: 30px;
            padding: 20px;
        }
        .order-card h5, .order-card p {
            margin: 0;
            margin-bottom: 10px;
        }
        .order-card ul {
            list-style: none;
            padding-left: 0;
        }
        .order-card ul li {
            border-bottom: 1px solid #ddd;
            padding: 10px 0;
            display: flex;
            justify-content: space-between;
        }
        .order-card ul li:last-child {
            border-bottom: none;
        }
        .btn-danger {
            background-color: #e74c3c;
            border: none;
        }
        .btn-danger:hover {
            background-color: #c0392b;
        }
        .dropdown-toggle {
              text-decoration: none;
              margin-left: 1200px;
              color: black;
              font-weight: bold;
              
        }
        

        .header__menu {
	text-align: center;
    
	padding: 26px 0 25px;
}

.header__menu ul li {
	list-style: none;
	display: inline-block;
	margin-right: 45px;
	position: relative;
}

.header__menu ul li.active a:after {
	-webkit-transform: scale(1);
	-ms-transform: scale(1);
	transform: scale(1);
}

.header__menu ul li:hover a:after {
	-webkit-transform: scale(1);
	-ms-transform: scale(1);
	transform: scale(1);
}

.header__menu ul li:hover .dropdown {
	top: 32px;
	opacity: 1;
	visibility: visible;
}

.header__menu ul li:last-child {
	margin-right: 0;
}

.header__menu ul li .dropdown {
	position: absolute;
	left: 0;
	top: 56px;
	width: 150px;
	background: #111111;
	text-align: left;
	padding: 5px 0;
	z-index: 9;
	opacity: 0;
	visibility: hidden;
	-webkit-transition: all, 0.3s;
	-o-transition: all, 0.3s;
	transition: all, 0.3s;
}

.header__menu ul li .dropdown li {
	display: block;
	margin-right: 0;
}

.header__menu ul li .dropdown li a {
	font-size: 14px;
	color: #ffffff;
	font-weight: 400;
	padding: 5px 20px;
	text-transform: capitalize;
}

.header__menu ul li .dropdown li a:after {
	display: none;
}

.header__menu ul li a {
	font-size: 18px;
	color: #111111;
	display: block;
	font-weight: 600;
	position: relative;
	padding: 3px 0;
}

.header__menu ul li a:after {
	position: absolute;
	left: 0;
	bottom: 0;
	width: 100%;
	height: 2px;
	background: #e53637;
	content: "";
    text-decoration: none;
	-webkit-transition: all, 0.5s;
	-o-transition: all, 0.5s;
	transition: all, 0.5s;
	-webkit-transform: scale(0);
	-ms-transform: scale(0);
	transform: scale(0);
}


    </style>
</head>
<body>
  <header>
    <nav class="header__menu mobile-menu">
        <ul >
          <li class="active" ><a href="/home.ejs" style="text-decoration: none;">Home</a></li>
          <li><a href="/shop" style="text-decoration: none;">Shop</a></li>
          
        </ul>
      </nav>
    <div class="col-lg-3 col-md-3">
      <div class="header__nav__option">
          <% if (locals.user) { %>
              <!-- User Dropdown Menu -->
              <div class="dropdown">
                  <a href="#" class="dropdown-toggle sign-in-link" id="userDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                      <%= locals.user.name %>
                  </a>
                  <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="userDropdown">
                      <li><a class="dropdown-item" href="/userProfile">Profile</a></li>
                      <li><hr class="dropdown-divider"></li>
                      <li><a class="dropdown-item" href="/logout">Logout</a></li>
                  </ul>
              </div>
          <% } else { %>
              <!-- Sign Up / Log In Links for Non-Logged-in Users -->
              <a href="/signup" class="sign-in-link">Sign Up</a>
              <a href="/login" class="sign-in-link">Log In</a>
          <% } %>
          
          
      </div>
  </div>


  
  
  <!-- Bootstrap JS for Dropdown functionality -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
  
  </header>
    <div class="container my-5">
        <div class="row">
            <!-- Sidebar -->
            <div class="col-md-3">
                <div class="dashboard-card">
                    <h5 class="card-title mb-4">Dashboard</h5>
                    <ul class="nav flex-column">
                        <li class="nav-item">
                            <a class="nav-link" href="#account-details">
                                <i class="fas fa-user"></i> Account Details
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/address">
                                <i class="fas fa-address-card"></i> Address
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="#coupons">
                                <i class="fas fa-ticket-alt"></i> Coupons
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/order/history">
                                <i class="fas fa-box"></i> Order History
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/cart">
                                <i class="fas fa-shopping-cart"></i> Shopping Cart
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="#track-order">
                                <i class="fas fa-truck"></i> Track Order
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="#wallet">
                                <i class="fas fa-wallet"></i> Wallet
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="#wishlist">
                                <i class="fas fa-heart"></i> Wish List
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="#logout">
                                <i class="fas fa-sign-out-alt"></i> Log-out
                            </a>
                        </li>
                    </ul>
                </div>
            </div>

            <!-- Main Content -->
            <div class="col-md-9">
                <h1 class="text-center mb-5">Your Order History</h1>
              
                <% if (orders.length > 0) { %>
                  <div>
                    <% orders.forEach(order => { %>
                      <div class="order-card">
                        <h5>Order ID: <%= order._id %></h5>
                        <p>Status: <span class="badge <%= order.status === 'Pending' ? 'bg-warning' : 'bg-success' %>"><%= order.status %></span></p>
                        <p>Placed on: <%= order.createdAtFormatted %></p>
                        
                        <h6 class="mt-3">Delivery Address:</h6>
                        <!-- <% if (orders.address) { %>
                          <p><%= order.address.name %>, <%= order.address.city %>, <%= order.address.state %> - <%= order.address.pincode %></p>
                        <% } else { %>
                          <p><em>Address not available</em></p>
                        <% } %> -->
                        <p><%= order.address %></p>
              
                        <h6>Items:</h6>
                        <ul>
                          <% order.items.forEach(item => { %>
                            <li>
                              <div>
                                <strong><%= item.productId.productName %></strong>
                                <div>Quantity: <%= item.quantity %></div>
                              </div>
                              <span>Total: <%= item.totalPrice %> $</span>
              
                              <% if (order.status === 'Pending') { %>
                                <form action="/order/cancel-item/<%= order._id %>/<%= item.productId._id %>" method="POST" onsubmit="return confirmCancel();">
                                  <button type="submit" class="btn btn-danger btn-sm">Cancel Item</button>
                                </form>
                              <% } %>
                            </li>
                          <% }) %>
                        </ul>
              
                        <p><strong>Total Order Price: <%= order.totalOrderPrice %> $</strong></p>
                        <a href="/order/details/<%= order._id %>" class="btn btn-dark">View Order Details</a> 
                      </div>
                    <% }) %>

                    
                  </div>
                <% } else { %>
                  <p class="text-center">No orders found.</p>
                <% } %>
              </div>
              
        </div>
    </div>

    <!-- Scripts -->
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
    
    <script>
        function confirmCancel() {
            return confirm("Are you sure you want to cancel this order?");
        }
    </script>
</body>
</html>
